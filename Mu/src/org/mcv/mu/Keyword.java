package org.mcv.mu;

public enum Keyword implements TokenType {
	NONE,
	IMPORT, FROM, AS, WHERE,
	TYPE, CLASS, 
	UNIT, SIUNIT,
	FUN, START,
	RETURN,
	THROW,
	PRINT, ASSERT,
	VAR, VAL, GET, // SET,
	IF, UNLESS, ELSE,
	SELECT, WHEN,
	DO, WHILE, UNTIL,
	BEFORE, AFTER, AROUND, ERROR, ALWAYS,
	FOR, IN,
	BREAK, CONTINUE,
	FALSE, TRUE, NIL,
	SQRT, ABS, XOR,
	INF, NAN,
	// type operators
	REF, LIST, SET, MAP, ENUM
}
